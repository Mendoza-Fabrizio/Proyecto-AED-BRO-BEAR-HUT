public class BroBearHubInventoryApp {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*Datos misDatos = new Datos();
        //Llamar a la interfaz de Login
        FrmLogin miLogin = new FrmLogin();
        miLogin.setDatos(misDatos);
        miLogin.setLocationRelativeTo(null);
        miLogin.setVisible(true);*/
		Usuario a = new Usuario();
		BSTree<Usuario = new BSTree<Usuario>();
		BSTree<Cliente = new BSTree<Cliente>();
		try {
			
		}
		catch (Exception y) {
			System.out.println(y.getMessage());
		}
	}
	public static void comparar(BSTree<Producto> a1, BSTree<Producto> a2) throws ItemNoFound{
		if(a1.search(null) == -1) {
			System.out.println();
		}
		else {
			return false;
		}
	}

}
